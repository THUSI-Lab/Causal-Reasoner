# QA Generators

This directory stores QA generation entry points.

## Layout

```text
stage_one_qa_generator/
  QA generation for single-step plan outputs.

stage_two_qa_generator/
  QA generation and evidence-path preparation for final plan item directories.
```

Post-generation filtering is maintained separately in `../../qa_filtering/`.
